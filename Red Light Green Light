import time
import board
import neopixel


pixels = neopixel.NeoPixel(board.NEOPIXEL, 1)

while True:
   
    pixels[0]=(255, 0, 0)
    time.sleep(1.5)
    pixels.fill((0, 255, 0))
    time.sleep(1.5)
